services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sfrfr
      POSTGRES_USER: sfrfr
      POSTGRES_PASSWORD: sfrfr
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
