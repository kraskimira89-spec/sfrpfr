## Техническое задание для Яндекс Cloud AI-ассистента

### Цель
Открыть SSH на ВМ `sfrfr-supabase-db-01` для актуального публичного IP агента cutover и подтвердить, что ВМ RUNNING и порты 80/443 слушаются.

### Контекст
- ВМ: `sfrfr-supabase-db-01` / `fhmkkkr7if3ggu6h29uq` / IP `51.250.69.237`
- Каталог: `b1grtprgfugidt9u073i`
- SG: `enpf27it4pe5d1brj6as` (`sfrfr-supabase-db-sg`)
- DNS `supabase.proverkastaza.ru` уже указывает на `51.250.69.237`; HTTPS с публичной сети отвечает.
- Актуальный публичный IP рабочей станции/агента: **`149.102.241.241/32`**
- Ранее в SG для 22 уже были: `146.158.1.12/32`, `185.77.216.28/32`, `185.77.216.16/32` — **не удалять**

### Шаги
1. Проверить статус ВМ `fhmkkkr7if3ggu6h29uq`: должен быть **RUNNING**, публичный IP **`51.250.69.237`**.
2. В SG `enpf27it4pe5d1brj6as` убедиться, что INGRESS TCP **80** и **443** ← `0.0.0.0/0` есть; если нет — добавить.
3. В SG добавить INGRESS TCP **22** ← **`149.102.241.241/32`** (описание: cutover agent SSH). Старые правила 22 **не удалять**.
4. Через serial console (если SSH снаружи всё ещё недоступен) выполнить и вернуть вывод:
   - `sudo ufw status`
   - `sudo ss -tlnp | grep -E ':80|:443|:22'`
   - `cd /opt/sfrfr-supabase/supabase/docker && docker compose ps`
5. Вернуть таблицу правил SG (только 22/80/443) и краткий статус ВМ.

### Ограничения
- Не менять DNS, не delete/stop ВМ, не трогать старый каталог `b1g0mhpm9tr4lrurk1bu`.
- Не открывать 5432/5433/8000 в `0.0.0.0/0`.
- Не читать секреты/.env/Lockbox.

### Критерии готовности
- ВМ RUNNING, IP `51.250.69.237`.
- Правило 22 для `149.102.241.241/32` есть; 80/443 публичны.
- Краткий отчёт + вывод команд из п.4 (если serial использовался).

---
Это техническое задание для выполнения Яндекс-ассистентом.
