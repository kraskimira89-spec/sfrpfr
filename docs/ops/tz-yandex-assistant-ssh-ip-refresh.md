## Техническое задание для Яндекс Cloud AI-ассистента

### Цель
Снова открыть SSH на ВМ `sfrfr-supabase-db-01` для IP агента cutover (публичный IP сменился).

### Контекст
- ВМ: `sfrfr-supabase-db-01` / `fhmkkkr7if3ggu6h29uq` / IP `51.250.69.237`
- Каталог: `b1grtprgfugidt9u073i`
- SG: `enpf27it4pe5d1brj6as` (`sfrfr-supabase-db-sg`)
- Сейчас SSH с рабочей станции таймаут. Актуальный IP администратора: **`146.158.1.12/32`** (домашний провайдер владельца, AS43849 Sever-Svyaz); запасной вход — jump через App-VPS **`91.229.11.147/32`**
- Адреса `185.77.216.28/32` и `185.77.216.16/32` (Creanova, выходы VPN) удалены из SG 2026-09-26 и **не возвращаются**
- Идёт перенос стека со старой ВМ `51.250.13.240` → эта ВМ; без SSH нельзя залить архив и поднять Compose

### Шаги
1. В SG `enpf27it4pe5d1brj6as` убедиться, что есть INGRESS TCP **22** ← **`146.158.1.12/32`**; если нет — добавить (описание: admin SSH home).
2. Существующие правила 22 (`146.158.1.12/32`, `91.229.11.147/32`) **не удалять**.
3. Вернуть таблицу правил 22 после изменения.

### Ограничения
- Не менять DNS, 80/443, не delete ВМ, не трогать старый каталог.
- Не добавлять SSH-правила на IP без проверки ASN (ipinfo.io). IP хостингов и VPN (тип proxy/hosting: M247, Creanova и т.п.) не допускаются. При сомнении — не открывать 22, а ходить через jump-хост App-VPS `91.229.11.147`.

### Критерии готовности
- Правило 22 для `146.158.1.12/32` есть в SG.
- Краткий отчёт.

---
Это техническое задание для выполнения Яндекс-ассистентом.
