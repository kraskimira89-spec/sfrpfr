# Тикет в техподдержку Рег.ру: DNS для proverkastaza.ru

Скопируйте блок ниже в обращение в поддержку (заказ №214839277).

---

Здравствуйте!

Прошу помочь настроить DNS для недавно оплаченных доменов (заказ №214839277) и проверить зоны. Все домены обслуживаются на NS Рег.ру (`ns1.reg.ru` / `ns2.reg.ru`). Сайт и сервисы размещены на нашем VPS.

**IP сервера (A-запись):** `91.229.11.147`

Прошу внести ресурсные записи **точно** как указано ниже. Лишние конфликтующие A/AAAA/CNAME на этих именах, пожалуйста, удалите или отключите (парковку/заглушку Рег.ру для этих хостов не оставлять).

### 1. Домен `proverkastaza.ru` (основной)

| Тип | Имя (хост) | Значение | TTL |
|-----|------------|----------|-----|
| A | `@` | `91.229.11.147` | по умолчанию |
| A | `www` | `91.229.11.147` | по умолчанию |
| A | `api` | `91.229.11.147` | по умолчанию |
| A | `cabinet` | `91.229.11.147` | по умолчанию |
| A | `admin` | `91.229.11.147` | по умолчанию |

Итоговые имена:  
`proverkastaza.ru`, `www.proverkastaza.ru`, `api.proverkastaza.ru`, `cabinet.proverkastaza.ru`, `admin.proverkastaza.ru` → `91.229.11.147`.

### 2. Домен `proverka-staza.ru` (алиас → основной сайт)

| Тип | Имя | Значение |
|-----|-----|----------|
| A | `@` | `91.229.11.147` |
| A | `www` | `91.229.11.147` |

HTTP(S)-редирект на `https://proverkastaza.ru/` настроим на нашем сервере.  
Если у вас удобнее включить **переадресацию домена** Рег.ру на `https://proverkastaza.ru/` (с сохранением пути, код 301) — можно так; тогда A на наш IP для этого домена не обязательны. Предпочтительно всё же A на `91.229.11.147`, чтобы мы выпустили SSL и сделали 301 сами.

### 3. Домен `prostaz.ru` (алиас → основной сайт)

Аналогично п. 2:

| Тип | Имя | Значение |
|-----|-----|----------|
| A | `@` | `91.229.11.147` |
| A | `www` | `91.229.11.147` |

Либо переадресация 301 на `https://proverkastaza.ru/` (с сохранением пути).

### 4. Старый домен `taxi-doroga-dobra.ru`

Пожалуйста, **не меняйте** существующие A-записи (`@`, `www`, `api`, `cabinet`, `admin` → тот же IP `91.229.11.147`), если они уже настроены. Редирект со старого домена на новый выполним на VPS после проверки DNS новых зон.

### 5. Просьба по итогам

После внесения изменений прошу в ответе подтвердить:

1. что записи из п. 1–3 созданы;
2. результат проверки: `proverkastaza.ru` и `api.proverkastaza.ru` резолвятся в `91.229.11.147`;
3. что для новых доменов отключена парковка/заглушка Рег.ру (если была).

Заранее спасибо за помощь!

С уважением,  
[Ваше имя]  
Аккаунт / логин в Рег.ру: [укажите]  
Заказ: №214839277  
Контакт для связи: [телефон / email]
