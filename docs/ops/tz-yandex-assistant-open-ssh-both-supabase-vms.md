## Техническое задание для Яндекс Cloud AI-ассистента

### Цель
Открыть SSH с рабочей станции администратора на **обе** ВМ Supabase (старая прод-DNS и новая cutover), добавив актуальный публичный IP в Security Groups. DNS, Lockbox и данные Postgres не менять.

### Контекст
- Организация Cloud: `proverkastaza` / id при необходимости смотреть в консоли.
- **Старая ВМ (сейчас DNS `supabase.proverkastaza.ru`):**
  - Имя: `sfrfr-staging-supabase`
  - Публичный IP: `51.250.13.240`
  - Каталог: `b1g0mhpm9tr4lrurk1bu` (облако `sfrfr-ai` / `b1gkscu5sqpjtf5d5rbi`)
  - Пользователь SSH: `sfrfr`
  - SG: группа этой ВМ в том же каталоге (Terraform-имя вида `sfrfr-…-staging`; найти по ВМ → сеть → security groups).
  - С ноутбука TCP/22 = timeout; с app-VPS `91.229.11.147` порт 22 уже доступен.
- **Новая ВМ (cutover, пустой стек):**
  - Имя: `sfrfr-supabase-db-01` / id `fhmkkkr7if3ggu6h29uq`
  - Публичный IP: `51.250.69.237`
  - Каталог: `b1grtprgfugidt9u073i`
  - SG: `enpf27it4pe5d1brj6as` (`sfrfr-supabase-db-sg`)
  - Пользователь SSH: `sfrfr`
- **Актуальный публичный IP администратора (обязательно добавить):** `37.120.217.114/32`
- Ранее в правилах встречались (не удалять без нужды): `185.77.216.28/32`, `185.77.216.16/32`, `146.158.1.12/32`, `109.252.100.99/32`.
- App-VPS (Postgres allowlist, не открывать наружу): `91.229.11.147/32`
- На старой ВМ прямой Postgres `:5433` уже восстановлен агентом Cursor (через jump). Ассистенту **не** нужно трогать Docker/Compose.
- Если на каталог `b1g0mhpm9tr4lrurk1bu` нет прав (PERMISSION_DENIED) — выполнить шаги только для доступного каталога и явно написать, какой каталог недоступен; для старого каталога права выдаёт владелец (см. критерий ниже).

### Шаги
1. Определить текущий аккаунт и каталоги, к которым есть доступ.
2. **Каталог `b1grtprgfugidt9u073i` — SG `enpf27it4pe5d1brj6as`:**
   - Добавить INGRESS TCP **22** ← `37.120.217.114/32` (описание: admin SSH 2026-09-25).
   - Существующие правила TCP/22 **не удалять**.
   - TCP **80** и **443** из `0.0.0.0/0` оставить/добавить, если ещё нет (нужны для будущего cutover HTTPS).
   - **Не** добавлять 5432/5433/8000 из `0.0.0.0/0`.
   - Опционально: TCP **5433** ← только `91.229.11.147/32` (app-VPS).
3. **Каталог `b1g0mhpm9tr4lrurk1bu` — SG старой ВМ `sfrfr-staging-supabase` (`51.250.13.240`):**
   - Добавить INGRESS TCP **22** ← `37.120.217.114/32`.
   - Убедиться, что TCP **5433** разрешён с `91.229.11.147/32` (уже должно быть для dbt/API).
   - Существующие allowlist-правила 22/5432/5433 **не сносить**.
   - **Не** открывать 22/5432/5433/8000 в `0.0.0.0/0`.
4. Вернуть **таблицы INGRESS** обеих SG после правок (порт, CIDR, описание).
5. Если нет IAM на `b1g0mhpm9tr4lrurk1bu`: кратко написать PERMISSION_DENIED и что владельцу нужно выдать `editor`/`vpc.admin` пользователю ассистента на этот каталог (или выполнить шаг 3 вручную).

### Ограничения
- Не менять DNS `supabase.proverkastaza.ru` и любые A-записи.
- Не останавливать / не удалять ВМ, диски, snapshot, Lockbox.
- Не читать и не менять секреты Lockbox / `.env` на ВМ.
- Не выполнять SSH и команды внутри ВМ (Docker, Compose, reboot) в рамках этого ТЗ.
- Не `terraform destroy`, не открывать Postgres в интернет.
- Не коммитить секреты.

### Критерии готовности
- В ответе: подтверждение правила TCP/22 для `37.120.217.114/32` в SG новой ВМ (`enpf27it4pe5d1brj6as`).
- То же для SG старой ВМ **или** явный PERMISSION_DENIED по каталогу `b1g0mhpm9tr4lrurk1bu`.
- Таблицы правил SG (без секретов).
- Краткий отчёт: что сделано / что недоступно.

---
Это техническое задание для выполнения Яндекс-ассистентом.
