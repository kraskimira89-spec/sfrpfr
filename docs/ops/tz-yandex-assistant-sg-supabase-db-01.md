## Техническое задание для Яндекс Cloud AI-ассистента

### Цель
Подготовить ВМ `sfrfr-supabase-db-01` к cutover домена `supabase.proverkastaza.ru` (DNS меняется вручную в reg.ru, не в YC).

### Контекст
- Каталог: `b1grtprgfugidt9u073i`
- ВМ: `sfrfr-supabase-db-01` / id `fhmkkkr7if3ggu6h29uq`
- Публичный IP: `51.250.69.237`
- Security group: `enpf27it4pe5d1brj6as` (`sfrfr-supabase-db-sg`)
- Сейчас INGRESS только TCP/22 с `146.158.1.12/32`
- Каталог `b1g0mhpm9tr4lrurk1bu`, Lockbox и старую ВМ `51.250.13.240` **не трогать**
- DNS зоны `proverkastaza.ru` в этом каталоге нет — A-запись ассистент **не** меняет

### Шаги
1. В SG `enpf27it4pe5d1brj6as` добавить INGRESS:
   - TCP **443** ← `0.0.0.0/0` (описание: HTTPS public)
   - TCP **80** ← `0.0.0.0/0` (описание: HTTP ACME/redirect)
   - TCP **22** ← `91.229.11.147/32` (описание: Admin SSH via app-VPS fallback; **добавить** к существующему `146.158.1.12/32`, не удалять его)
2. **Не** добавлять INGRESS на 5432, 5433, 8000 из `0.0.0.0/0`.
3. Показать итоговую таблицу правил SG после изменения.
4. Опционально (если есть право и известен CIDR app-VPS): TCP **5433** только с `91.229.11.147/32` — иначе пропустить и кратко написать «пропущено».

### Ограничения
- Не менять DNS.
- Не делать SSH и не выполнять команды на ВМ.
- Не читать Lockbox.
- Не открывать Postgres в интернет.
- Не удалять и не останавливать ВМ.
- Не добавлять SSH-правила на IP без проверки ASN (ipinfo.io). IP хостингов и VPN (тип proxy/hosting: M247, Creanova и т.п.) не допускаются. При сомнении — не открывать 22, а ходить через jump-хост App-VPS `91.229.11.147`.

### Критерии готовности
- В ответе: подтверждение, что 80 и 443 открыты; 22 содержит оба `/32`; 5432/5433/8000 не публичны.
- Таблица всех INGRESS/EGRESS правил SG после правок.

---
Это техническое задание для выполнения Яндекс-ассистентом.
