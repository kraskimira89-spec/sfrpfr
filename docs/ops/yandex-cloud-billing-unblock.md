# Яндекс.Облако: разблокировка биллинга (AI Studio / ресурсы)

**Облако:** `cloud-infoproverkastazaru`  
**Статус на 2026-07-28:** блокер — баннер «Облако заблокировано», баланс **0,00 ₽**.  
Пока блок не снят — **не** создавать сервисный аккаунт / API-ключ AI Studio (шаг 1 остановлен).

> Это **не** Яндекс Workspace (почта/Диск/Календарь/Телемост) — OAuth ТЗ-14 живёт отдельно и уже работает.

---

## Сейчас

1. Форму **«Консультация и расчёт гранта»** — закрыть, если грант не нужен (это не разблокировка).
2. Разблокировать биллинг (**вариант A** ниже).

### Вариант A — пополнить (нужен для AI Studio)

1. В шапке консоли — баланс **0,00 ₽** или [Биллинг](https://console.yandex.cloud/billing).
2. Платёжный аккаунт облака `cloud-infoproverkastazaru`.
3. Статус не `SUSPENDED` / не «заблокирован».
4. Пополнить (даже небольшая сумма).
5. Обновить главную: баннер «Облако заблокировано» исчез.

📖 [Биллинг](https://yandex.cloud/ru/docs/billing/) · [Проблемы с платежами](https://yandex.cloud/ru/docs/billing/qa/billing-account)

### Вариант B — грант (опционально)

Только если нужен грант миграции. Иначе вкладку закрыть.  
Грант **не заменяет** активный биллинг для API.

---

## После снятия блока — шаг 1 (AI Studio ключ)

1. Каталог: `default` или `sfrfr-ai` → скопировать `folder_id` из URL.
2. IAM → Сервисные аккаунты → SA + роль `ai.languageModels.user`.
3. API-ключ со scope `yc.ai.languageModels.execute`.
4. Ключ **не** в чат / **не** в git → `YANDEX_API_KEY` / `YANDEX_FOLDER_ID` в `.env` / VPS.

Кнопка **«Перейти в AI Studio»** — шаг 2 (тест в UI), когда ключ уже есть.

---

## Чеклист

- [ ] Баланс / статус платёжного аккаунта ок (не 0 / не suspended)
- [ ] Баннер «Облако заблокировано» исчез
- [ ] Форму гранта закрыли или осознанно отправили
- [ ] _(дальше)_ SA + API-ключ AI Studio
