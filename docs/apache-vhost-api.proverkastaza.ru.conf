# API SFRFR: api.proverkastaza.ru → uvicorn 127.0.0.1:8011

<VirtualHost *:80>
    ServerName api.proverkastaza.ru

    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:8011/
    ProxyPassReverse / http://127.0.0.1:8011/

    RequestHeader set X-Forwarded-Proto "http"
    RequestHeader set X-Forwarded-For "%{REMOTE_ADDR}s"

    ErrorLog ${APACHE_LOG_DIR}/api.proverkastaza.ru-error.log
    CustomLog ${APACHE_LOG_DIR}/api.proverkastaza.ru-access.log combined
</VirtualHost>
