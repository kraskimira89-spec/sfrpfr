# 2026-09-25 — VM C :5433 + ТЗ SSH allowlist

## Сделано локально (Cursor)
- Jump `root@91.229.11.147` → `sfrfr@51.250.13.240`: у running `supabase-db` не было host `:5433`, хотя override в COMPOSE_FILE.
- `docker compose up -d db` → `0.0.0.0:5433->5432`; с VPS `psql` OK (`select 1`).
- `terraform.tfvars` (gitignore): `allowed_ssh_cidrs = ["37.120.217.114/32"]` (текущий IP; apply не делали). **Удалено 2026-09-26:** выход VPN (AS9009 M247), не IP владельца — см. `docs/history/2026-09-25-yc-sg-ssh-cleanup.md`.

## ТЗ ассистенту
`docs/ops/tz-yandex-assistant-open-ssh-both-supabase-vms.md` — добавить `37.120.217.114/32` в SG обеих ВМ. **Удалено 2026-09-26:** выход VPN (AS9009 M247), не IP владельца — см. `docs/history/2026-09-25-yc-sg-ssh-cleanup.md`.

## Не сделано
- Прямой SSH с ноутбука на `51.250.13.240` (нужен SG).
- Terraform apply в YC.
