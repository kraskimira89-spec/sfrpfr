# 2026-09-26 — ТЗ-35 B1: анкета нового клиента в MAX

- После «Начать» новый клиент проходит 8 вопросов §6: фамилия, имя, отчество («Нет отчества»),
  год рождения, стаж (кнопки), телефон, e-mail («Пропустить»), описание проблемы (до 2000 символов).
- Существующие клиенты (приветствие уже было, прежний intake завершён или передан оператору,
  в `clients` есть `birth_year`) — прежний сценарий без анкеты.
- Файл посреди анкеты принимается, затем бот повторяет текущий вопрос.
- Итог: `clients` (ФИО, телефон, e-mail, `birth_year`), `cases` (`experience_bucket`, `problem_text`,
  `questionnaire_completed_at`), сводка анкеты — системной записью в ленте дела.
- После анкеты — «Ваше дело создано» и меню: загрузить документы / какие документы нужны /
  статус дела / связаться со специалистом. Номер CASE — в D1, чек-лист после анкеты — в B2.
- Откат без деплоя: `MAX_QUESTIONNAIRE_ENABLED=0` в `.env` VPS.
- Миграция `20260926170000_questionnaire_b1.sql`: пробный прогон на db-01 с ROLLBACK — успешно.
