# 2026-08-13 — очередь «комментариев» = pingback

## Откуда

48 записей в «На утверждении» — не спам-боты, а **pingback** WordPress:
статьи блога ссылались друг на друга при сиде; сервер (`91.229.11.147`) слал себе уведомления.
Автор = заголовок статьи, URL = ссылка на пост, agent = XML-RPC WordPress.

Комментарии и пинги уже были `closed` по умолчанию; очередь накопилась раньше.

## Что сделано

- Удалены все 48 pingback + демо-комментарий ID 1
- `default_pingback_flag=0`, ping/comment closed на опубликованных постах/страницах
