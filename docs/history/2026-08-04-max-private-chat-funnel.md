# 2026-08-04 — ТЗ-20: CTA сайта + воронка личного чата MAX

## Сделано

- Главные CTA сайта: «Начать проверку в MAX» → `MAX_CHAT_URL` (личный чат).
- Убраны конкурирующие первичные ссылки на кабинет/канал в hero.
- Бот: `/start` показывает меню целей **без** создания дела.
- Диагностика `intake:*` (цель → ИЛС → трудовая → устройство) → резюме → deep-link кабинета с `case_id`.
- Ветка «Позвать специалиста» + best-effort задача в amoCRM.
- Production: вложения в чат отклоняются с кнопками кабинета.
- Миграция `max_intake`, файловый store для локали/тестов.
- Автотесты `tests/unit/test_max_intake.py` + обновление старых MAX-тестов.

## Осталось (из ТЗ-20)

- Регистрация меню команд `PATCH /me/commands` при поддержке API.
- Полный sync `max_intake` в Postgres из бота (сейчас файл + миграция).
- События аналитики `max_chat_open` / `max_intake_*`.
- Ручной E2E на телефоне.
