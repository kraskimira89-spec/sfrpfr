# Клиентский кабинет Next.js: cabinet.proverkastaza.ru → 127.0.0.1:3001
# Перед SSL: a2ensite + certbot --apache -d cabinet.proverkastaza.ru

<VirtualHost *:80>
    ServerName cabinet.proverkastaza.ru

    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:3001/
    ProxyPassReverse / http://127.0.0.1:3001/

    RequestHeader set X-Forwarded-Proto "http"
    RequestHeader set X-Forwarded-For "%{REMOTE_ADDR}s"

    ErrorLog ${APACHE_LOG_DIR}/cabinet.proverkastaza.ru-error.log
    CustomLog ${APACHE_LOG_DIR}/cabinet.proverkastaza.ru-access.log combined
</VirtualHost>
