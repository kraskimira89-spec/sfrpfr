# Админ-кабинет Next.js: admin.proverkastaza.ru → 127.0.0.1:3002
# Перед SSL: a2ensite + certbot --apache -d admin.proverkastaza.ru

<VirtualHost *:80>
    ServerName admin.proverkastaza.ru

    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:3002/
    ProxyPassReverse / http://127.0.0.1:3002/

    RequestHeader set X-Forwarded-Proto "http"
    RequestHeader set X-Forwarded-For "%{REMOTE_ADDR}s"

    ErrorLog ${APACHE_LOG_DIR}/admin.proverkastaza.ru-error.log
    CustomLog ${APACHE_LOG_DIR}/admin.proverkastaza.ru-access.log combined
</VirtualHost>
